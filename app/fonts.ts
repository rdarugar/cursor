import { Instrument_Sans, Abril_Fatface, Bebas_Neue } from 'next/font/google';

export const instrumentSans = Instrument_Sans({
  subsets: ['latin'],
  display: 'swap',
});

export const abrilFatface = Abril_Fatface({ 
  weight: '400',
  subsets: ['latin'],
});

export const bebasNeue = Bebas_Neue({
  weight: '400',
  subsets: ['latin'],
}); 